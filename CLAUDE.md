# CLAUDE.md

**작업 전에 [CONTEXT.md](CONTEXT.md) 를 먼저 읽으세요.** 구조·근거·지뢰는 전부 거기 있습니다.
아래는 규칙만 적습니다 — 이유가 궁금하면 CONTEXT.md 를 보세요.

## 작업 방식

- **코드를 쓰기 전에 계획을 먼저 설명하고 승인을 받을 것.**
- **파일을 수정할 때마다** `git add -A && git commit -m "WIP: <무엇>"`.
  이 PC 가 예고 없이 재부팅됩니다. **푸시는 지시할 때만.**
- `firestore.rules` 를 건드릴 일이 생기면 **먼저 근거를 말할 것.**

## 스스로 검토할 것

외부 검토자가 없습니다. 지금까지 남이 잡아 주던 것을 스스로 잡아야 합니다.

- 계획을 세운 뒤, **실행 전에 그 계획의 약한 지점을 스스로 짚을 것.**
  "이 판단이 틀렸다면 어디서 틀렸을까" 를 한 번 묻고 답한 뒤 진행한다.
- **가설과 코드가 실제로 일치하는지 확인할 것.**
  화면만 보고 원인을 추정하지 말고 코드를 읽어서 확인한다.
- **실측이 필요한 값을 예상값으로 대체하지 말 것.** 잴 수 없으면 "못 쟀다" 고 말한다.
- **검증 환경이 실제 환경과 다르면 그 사실을 먼저 밝힐 것.**
  데스크톱 통과는 폰의 근거가 안 되고, dpr 1 통과는 다른 배율의 근거가 안 된다.
- **사용자가 준 전제가 틀렸다고 판단되면 그대로 따르지 말고 반박할 것.**
- CONTEXT.md 를 갱신할 때는 늘리지 말고 **낡은 내용을 지우면서** 할 것 (갱신 시점은 아래 「문서 규칙」).

## 코드 규칙

- 스크립트 로드 순서 유지: `i18n → legal → auth → calendar → export → todo → firebase(module)`.
- **`js/firebase.js` 외에 새 ESM 모듈 금지.**
- 클릭·입력은 기존 위임에 `case` 추가. **개별 `addEventListener` 금지.**
- 입력은 **uncontrolled** 유지 (캐럿 보존).
- 새 i18n 키는 **ko/en 양쪽에**.
- CSS 는 `background:` 단축 금지. **`background-color:`** 를 쓸 것.
- **`_ds/` 는 원본 그대로.** `css/style.css` 에서 해결.
- 시트가 열려 있을 때 `render()` 를 미루는 가드 유지.
- 새 시트를 만들면 **`sheetBusy()` 와 `applyLoggedOut()` 양쪽에** 반영.

## selftest

- 단언의 기댓값을 **조건식으로 옮겨 적지 말 것** (항등식이 됨).
- `getComputedStyle` 의 px 을 **리터럴로 비교하지 말 것** (브라우저 확대에서 소수).
- **검사 개수를 손으로 세지 말 것.**
- 무엇을 보고 판정할지는 아래 「검증 방법」을 따를 것.

# 불변 규칙 — 어기면 이미 고친 버그가 재발한다

- IMPORTANT: 여러 날 일정의 밑줄은 inset box-shadow다. border로 바꾸지 마라.
  border-bottom은 box-sizing:border-box 때문에 height:17px 안에서 글자 자리를 3px 빼앗아 글자가 잘린다.
  box-shadow는 레이아웃을 건드리지 않아 BAR_H, lane 계산, barSpacer가 전부 그대로 남는다.
- IMPORTANT: hidden 속성은 인라인 display에 진다. 숨김과 보임은 display를 직접 토글한다.
  검사도 el.hidden이 아니라 getComputedStyle(el).display를 본다.
  이 규칙이 없어서 메모 있는 줄마다 펼침 화살표가 다 뜬 적이 있고, selftest는 그걸 통과시켰다.
- 카테고리 원색을 판으로 채우는 화면을 다시 만들 때 글자를 흰색으로 고정하지 마라.
  팔레트의 밝은 쪽(#34C759, #00C7BE, #FF9500)에서 대비가 1.8~2.3:1로 읽히지 않는다.
- eventsFirst()는 아래 목록과 일간 이미지 두 곳에서만 호출한다.
  sortItems나 itemsOn 안으로 옮기면 월간과 주간 칸의 알약과 막대 층이 흔들린다.
- 아젠다 한 줄의 높이 계산은 exRowH() 하나로만 한다. 세 곳에 흩뜨리지 마라.
- saveTodo는 merge이고 saveCat은 통째 교체다. 혼동하면 필드가 조용히 사라진다.
- order는 항목마다 하나다. 날짜별이 아니다. 한 날에서 바꾼 순서는 그 항목이 나오는 모든 날에 적용된다.
- robots.txt에서 css/, js/, _ds/를 막지 마라. 이 앱은 HTML에 내용이 없어 막으면 구글이 빈 화면으로 읽는다.
- index.html의 Search Console 소유확인 meta 태그를 지우지 마라. 지우면 소유 확인이 풀린다.

# 검증 방법

- 확인은 그려진 DOM으로 한다. 속성값이 정직해도 화면은 다를 수 있다.
- Grep 출력의 슬래시를 믿지 마라. 닫는 태그와 주석 기호가 뭉개져 보인다. 고칠 대상은 Read로 확인한다.
- 뷰포트는 1280x900을 명시하고 잰다. 창이 없으면 0x0으로 접혀 검사가 엉뚱하게 실패한다.
- 코드를 고친 뒤 확인할 때는 캐시를 비우고 새로고침한다. 브라우저가 js/*.js를 캐시에서 쓴다.
- selftest는 브라우저에서 ?selftest로 돈다. 실패하면 콘솔에 throw가 뜬다.
  결과를 보고할 때는 통과 개수와 실패한 검사 이름을 출력 그대로 옮긴다. 요약하지 않는다.
  현재 기준선은 432개 전부 통과다.
- selftest CLI 러너가 만들어진 뒤에는 러너의 명령과 stdout 마지막 줄과 exit code를 함께 보고한다.
- 브라우저에서 눈으로 확인하지 않은 것을 완료라고 보고하지 마라.
  스스로 완료라고 한 것과 사용자가 화면에서 본 것이 다른 적이 여러 번 있었다.

# 게이트 우회 금지

- IMPORTANT: 파일 게이트를 피하려고 파일 편집을 서브에이전트에 넘기지 마라.
  서브에이전트 호출은 파일 게이트를 건너뛴다. 이것은 우회로이지 허가가 아니다.
  서브에이전트는 조사와 검토에만 쓰고, 코드 편집은 본 세션에서 게이트를 지나서 한다.
- 파일 쓰기는 Write 도구를 쓴다. cat > file 은 Bash 게이트를 지나 매번 비용이 든다.
- 문서 본문에 git checkout, git reset --hard 같은 문자열을 heredoc으로 넣지 마라.
  게이트가 본문을 명령으로 오탐한다. 복구 절차는 Write 도구로 쓰거나 서술형으로 적는다.

# 문서 규칙

- CONTEXT.md가 구조와 규칙의 정본이다. 작업정리 문서는 이력이다.
- 작업을 끝내면 CONTEXT.md §6 잔여 목록을 갱신한다.
- 작업정리 문서에는 "실제로 잰 것"과 "못 잰 것"을 반드시 나눠 적는다. 추측으로 채우지 않는다.

# Compact Instructions

When compacting, always preserve:
- 위 불변 규칙 전체 (요약하지 말 것)
- 이번 세션에서 수정한 파일 전체 목록
- selftest 명령과 가장 최근 통과 개수
- 아직 끝나지 않은 작업 번호
- "못 잰 것" 목록
