# 작업 정리 — 2026년 8월 말 ~ 9월 1일

이번 회차에 무엇을 왜 바꿨는지, **무엇을 실제로 재서 확인했고 무엇을 못 했는지** 를
남깁니다. 구조·규칙은 [CONTEXT.md](CONTEXT.md) 가 정본이고 이 문서는 이력입니다.

배포는 모두 끝났습니다 (`origin/main` = 로컬, GitHub Pages → `todo-calendar.kro.kr`).

---

## 1. 한눈에

| 묶음 | 무엇 |
|---|---|
| **디자인 되돌림** | 옛 달력풍 리디자인을 전부 취소하고 원래 iOS 풍으로 복귀 |
| **여러 날 일정** | 원색 판 → **색 밑줄 + 잉크 글자**, 월간 날짜 오른쪽 정렬 |
| **순서 끌기** | 카테고리에 이어 **할 일도 끌어서 순서 변경**, 두 목록이 한 엔진 |
| **아래 목록** | 일정/할 일 묶기 + 구분선, 카테고리 이름, 메모 펼치기 |
| **내보내기** | 카테고리·메모 포함 토글(기본 꺼짐), 줄 높이 계산 일원화 |
| **SEO** | `robots.txt`·`sitemap.xml`·`canonical`, Search Console 등록 완료 |

selftest 는 **403 → 432개** 로 늘었고 전부 통과합니다.

---

## 2. 기능별 상세

### 2-1. 디자인 되돌림

옛 한국 벽걸이 달력 → 손글씨 간판 풍으로 앱 전체를 리디자인했다가, 사용자 판단으로
**완전히 원래대로** 되돌렸습니다(`07f5799` 기준).

- 되돌린 18커밋은 브랜치 **`design-signboard-full`** (`29f83e4`) 에 보존
- 되돌아갈 태그 둘: **`design-before-vintage`** (`07f5799`), **`design-before-signboard`** (`8217d95`)
- 셋 다 **원격에 올려 두었습니다** — 이 PC 가 죽어도 남습니다
- ⚠️ 그 브랜치 이력에 **페이퍼로지·조선로고체 폰트 파일** 이 들어 있고, 저장소가
  공개라 함께 공개됐습니다. 폰트 배포 조건은 **확인하지 않았습니다.** 재배포가
  걸리는 라이선스면 브랜치를 지우고 폰트 없는 이력으로 다시 올려야 합니다.

### 2-2. 여러 날 일정 — 밑줄 (`ebb4d66`, `b072b80`)

원색 판 위 글자에서 **색 밑줄 + 잉크 글자** 로 바꿨습니다. 하루짜리는 그대로 색 점 +
색 글자라, 이제 둘이 "밑줄이냐 점이냐" 로 갈립니다.

- 밑줄은 `border-bottom` 이 **아니라 inset box-shadow** 입니다. `box-sizing:border-box`
  라 테두리는 `height:17px` 안에서 글자 자리를 3px 빼앗아 글자가 잘립니다.
  box-shadow 는 레이아웃을 한 픽셀도 안 건드려 `BAR_H`·lane 계산·`barSpacer` 가 전부
  그대로 남습니다. **border 로 바꾸지 마세요.**
- 월간 뷰 날짜만 오른쪽 정렬(주간은 머리 줄 한가운데라 그대로).
- 색 판이 사라지며 `onColor()`(휘도로 글자색 고르던 함수)의 마지막 호출처가 없어져
  함수와 전용 검사 2개를 삭제했습니다.
  ⚠️ 카테고리 원색을 **판으로 채우는** 화면을 다시 만들면 흰 글자로 고정하지 마세요 —
  팔레트의 밝은 쪽(`#34C759`·`#00C7BE`·`#FF9500`)에서 대비가 1.8~2.3:1 로 안 읽힙니다.

### 2-3. 순서 끌기 (`b20bce9`, `7084f68`)

**카테고리** — 세 줄 손잡이를 누르면 기다림 없이 잡히고 줄이 살짝 떠오릅니다
(`scale 1.03` + 그림자, `.16s`). 줄 본문은 그대로 400ms 길게 눌러야 잡힙니다.
여기까지 즉시로 만들면 폰에서 스크롤이 전부 순서 바꾸기가 됩니다.

**할 일** — 같은 손잡이를 아래 목록에도 넣었고, **두 목록이 끌기 엔진 하나** 를 씁니다.
다른 것은 셋뿐이라 그것만 인자로 받습니다: 어떤 줄인가(`sel`), 넘어갈 수 없는
경계(`lo`·`hi`), 놓았을 때 무엇을 하는가(`drop`).

- 바뀐 순서는 **달력 칸까지** 반영됩니다(사용자 요청). `sortItems` 가 `order` 를 제일
  먼저 보기 때문입니다. **같이 따라오는 것**: `weekEventBars` 가 이 순서로 막대 층을
  배정하므로, 일정 순서를 바꾸면 달력의 가로 막대가 위아래로 움직입니다.
- 끌기는 **제 무리 안에서만** 됩니다(일정끼리 / 할 일끼리). 굵은 구분선을 넘으면
  "선 위 = 일정" 이 거짓이 됩니다. 무리에 혼자면 손잡이를 아예 안 그립니다.
- `order` 는 **항목마다 하나** 입니다(날짜별이 아님). 반복·여러 날 항목은 여러 날에
  나타나지만 문서는 하나라, **한 날에서 바꾼 순서가 그 항목이 나오는 모든 날에**
  적용됩니다. 날짜별로 두려면 항목마다 날짜→번호 맵이 필요해 문서가 계속 자랍니다.
- **보안 규칙 변경이 필요 없었습니다.** `todos` 규칙은 `allow read, write` 뿐이고
  필드 제약(`hasOnly`)이 없습니다 — categories·goals 와 다른 점입니다.
- `saveTodo` 가 `merge` 라 순서 저장은 **`{ order: n }` 한 필드만** 보냅니다.
  `saveCat`(통째 교체)과 정반대라 "안 실어 보내면 조용히 날아가는" 함정이 없습니다.

### 2-4. 아래 목록 (`ddb4654`, `d9ddab1`, `26befb8`)

- **일정을 위로 묶고 경계에 굵은 선** (화면 3px · 이미지 7px). 안 묶으면 09시 할 일이
  14시 일정 위로 올라와 두 종류가 섞여 선 하나로 가를 자리가 없습니다. 묶는 일은
  `eventsFirst()` 가 **아래 목록과 일간 이미지 두 곳에서만** 합니다 —
  `sortItems`/`itemsOn` 안으로 옮기면 월·주 칸의 알약과 막대 층이 흔들립니다.
- **둘째 줄 = `[카테고리(그 색)] · [메모]`.** 카테고리가 없는 항목엔 이름을 안 붙입니다
  (폴백이 '없음' 이라 분류 안 한 줄마다 "없음 · …" 이 되어 잡음).
- **메모는 접히면 첫 줄만, 펼치면 `white-space:pre-wrap` 으로 줄바꿈 그대로.**
- 펼침 화살표는 **뒷줄이 있거나 첫 줄이 실제로 넘칠 때만** 뜹니다.

### 2-5. 내보내기 (`d9ddab1`)

- `메모 포함` → **`카테고리·메모 포함`** 으로 넓히고 **기본값을 껐습니다.** 이미지는
  남에게 보내는 것이라, 켜짐이 기본이면 한 번의 부주의로 사적인 메모가 나갑니다.
- 아젠다 한 줄의 높이를 **`exRowH()` 하나** 로 합쳤습니다. 예전엔 같은 식이 일간 배치·
  상세 배치·그리기 **세 곳에 따로** 적혀 있었고, 둘째 줄 조건이 `memo` 하나가 아니게
  되면서 하나만 고치면 글자가 다음 줄을 밟습니다. **다시 흩뜨리지 마세요.**

### 2-6. SEO (`7891b26`, `bf5fc65`)

| 파일 | 내용 |
|---|---|
| `robots.txt` | 크롤 허용 + 사이트맵 위치. **`css/`·`js/`·`_ds/` 를 안 막습니다** |
| `sitemap.xml` | 실제 페이지 4개, `lastmod` 는 손으로 적음 |
| `canonical` | 4개 페이지 전부 |

- `css`·`js` 를 막으면 안 되는 이유: 이 앱은 HTML 에 내용이 없고 스크립트가 화면을
  만듭니다. 막으면 구글이 **빈 화면으로 읽습니다.**
- `robots.txt` 의 Disallow 는 **비밀 유지 장치가 아닙니다.** 저장소가 공개라 그 파일들은
  GitHub 에서 그대로 읽힙니다. "검색 결과에 안 뜨게" 까지입니다.
- **Google Search Console 등록 완료** — 소유 확인 meta 태그는 `index.html` 에 있습니다
  (`bf5fc65`, 사용자 직접 작업). ⚠️ **그 태그를 지우면 소유 확인이 풀립니다.**
- 사이트맵 제출 결과: 상태 **성공**, 발견된 페이지 **4**.

---

## 3. 지운 것 — 되살리는 법

| 지운 것 | 커밋 | 되살리기 |
|---|---|---|
| `할일캘린더.html`, `Todo Calendar.dc.html` | `7891b26` | `git checkout 6c98f75 -- "할일캘린더.html"` |
| `legacy/`, `support.js`, `.thumbnail` | `08b3a21` (사용자) | `git checkout 08b3a21^ -- legacy support.js` |
| `tools/og.html` | `49429cc` | 이력에서 복구 (`og.png` 는 유지) |

`할일캘린더.html` 을 지운 이유: **제목이 `할 일 캘린더` 로 index.html 과 똑같아** 검색이
그쪽을 대표 페이지로 고를 수 있었고, 그러면 방문자가 7월 25일자 옛 화면을 봅니다.
둘 다 Firebase 를 안 쓰고 사이트 어디서도 링크되지 않은 것을 확인했습니다.

⚠️ `legacy/index-single-file.html` 도 이제 없습니다. 옛 단일 파일 원본을 보려면
**커밋 `6c98f75`** 를 봐야 합니다.

---

## 4. 실제로 잰 것 / 못 잰 것

이 항목이 이 문서에서 제일 중요합니다. **잰 것만 잰 것입니다.**

### 잰 것

- 순서 끌기: 손잡이를 누른 **직후** 잡힘, 줄 높이 44.4px **불변**(손잡이 있을 때·없을 때
  같음), 아이콘 간격 12px 유지, 뜬 줄이 시트 여백 안에 12px 남김(가로 스크롤바 없음)
- 뜨는 애니메이션 끝값: `scale 1.03` · 그림자 `rgba(0,0,0,.14) 0 8px 24px` · 반경 10px
- 구분선: 화면 경계 3px vs 실선 1px, 다크 모드 색도 정상 전환
- 내보내기: 토글 끔 → `cat`·`memo` 키 **없음**·높이 700 / 켬 → 키 생기고 높이 754,
  카테고리 글자가 **카테고리 색 픽셀로** 찍힘(빨강 168 → 404)
- 배포 사이트 실측: 카테고리 6개가 `order` 0~5 를 서버에서 받아옴, 할 일 85개 중 8개에
  `order` — 즉 **보안 규칙이 이미 배포돼 있음**
- SEO: 실제 도메인에서 `robots.txt` 200, `sitemap.xml` 200 + XML 파싱 오류 없음,
  canonical 4개 정상, 지운 파일 404

### 못 잰 것 (그대로 남은 위험)

- **폰 실기기 터치.** 브라우저 창이 안 떠서 합성 pointer 이벤트로만 확인했습니다.
  특히 **끌 때 화면이 같이 굴러가지 않는지**(손잡이의 `touch-action:none`)는 기기에서
  봐야 압니다. 이후 사용자가 폰에서 "할 일 이동·메모 펼치기 잘 된다" 고 확인했습니다.
- **애니메이션이 눈에 부드러운지.** 탭이 `hidden` 이라 애니메이션 시계가 0 에 멈춰,
  `getAnimations()` 로 160ms 를 직접 돌려 **끝값만** 확인했습니다.
- **폰트 라이선스.** `design-signboard-full` 브랜치에 실린 폰트의 재배포 조건.
- **배포된 보안 규칙 원문.** CLI 에 조회 명령이 없고 `gcloud` 도 없어, 저장된 데이터로
  **역추적** 했습니다.

---

## 5. 이번에 밟은 지뢰 — 다시 밟지 마세요

1. **`hidden` 속성이 인라인 `display` 에 진다.** 펼침 버튼에 `display:flex` 를 인라인으로
   주고 `hidden` 만 세웠더니 **화면에서 하나도 안 숨었습니다.** 메모 있는 줄마다 화살표가
   다 떴고 사용자가 폰에서 발견했습니다. → `display` 를 **직접** 토글합니다.
2. **검사가 속성만 보면 이런 걸 못 잡는다.** 위 문제를 selftest 가 통과시킨 이유는
   `b.hidden`(속성)을 봤기 때문입니다. 값은 정직하게 `true` 였고 화면만 아니었습니다.
   → 이제 `getComputedStyle(...).display` 를 봅니다. **그려진 DOM 을 보세요.**
3. **브라우저가 `js/*.js` 를 캐시에서 쓴다.** 새 코드를 올렸는데 옛 코드가 도는 걸 한참
   뒤에 알았습니다. 확인할 때는 **캐시 비우고 새로고침**(Ctrl+Shift+R).
4. **뷰포트가 0×0 이 될 수 있다.** 브라우저 창이 안 떠 있으면 크기가 접혀 selftest 가
   엉뚱하게 실패합니다. 크기를 **명시** 하고 재세요(1280×900).
5. **Grep 출력의 슬래시를 믿지 말 것.** 닫는 태그와 주석 기호가 뭉개져 보여, 있지도 않은
   버그를 있다고 판단할 뻔했습니다. 고칠 대상은 **Read 로** 확인하세요.

---

## 6. 남은 작업

| | 항목 | 메모 |
|---|---|---|
| **B** | PWA (`manifest.json`·아이콘·서비스 워커) | CDN 3곳 의존 → 캐싱 전략 필요 |
| **C** | PWABuilder → TWA → 갤럭시 스토어 | `/.well-known/assetlinks.json` |
| **E** | 애드센스 | 로그인 벽이라 색인될 글이 적음 — 로그인 전 소개 글이 도움 |

그 밖에 [CONTEXT.md](CONTEXT.md) §6 에 남아 있는 것:

- **`LEGAL.version` 이 아직 `"1.0"`** 인데 처리방침 본문은 그 뒤로 여러 번 바뀌었습니다.
  실사용자를 받기 전에 버전을 올릴지 재동의를 받을지 정하세요.
- **만 14세 미만 가입을 허용** 해 두어 스토어 아동 정책 심사가 까다로워집니다.
- 옛 `pri` 값 이관 여부 미정.
