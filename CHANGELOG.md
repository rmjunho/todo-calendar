# 변경 사항 정리

대상 파일: `untitled/project/index.html` (단일 파일, 빌드 도구 없음)
기준 디자인: `Todo Calendar.dc.html` + `_ds/ios-26-design-system-.../`

---

## 1. 디자인 시안 구현 (1차 작업)

Claude Design 핸드오프 번들의 `Todo Calendar.dc.html`은 전용 런타임(`<x-dc>`,
`<sc-for>`, `DCLogic`)에 의존해서 브라우저에서 바로 실행되지 않습니다.
그래서 **동작하는 결과물**로 `index.html`을 새로 만들었습니다.

| 항목 | 내용 |
|---|---|
| 기술 스택 | 순수 HTML + CSS + 바닐라 JS (프레임워크·빌드 없음) |
| 디자인 토큰 | `_ds/.../styles.css` 를 `<link>` 로 **그대로 재사용** (색상/타이포/간격/이펙트) |
| 다크 모드 | `colors.css` 가 `:root[data-theme="dark"]` 를 쓰므로 `<html>` 에 `data-theme` 지정 |
| 저장소 | `localStorage` |

### 이식한 디자인 시스템 컴포넌트

`_ds_bundle.js` 에서 필요한 4개만 CSS로 포팅했습니다 (SVG 패스는 원본 그대로).

- **Button** — small(h34) / medium(h44) / large(h50), prominent·tinted·gray·plain·glass
- **Switch** — 51×31 트랙, 27px 노브, `translateX(20px)`
- **PickerButton** — 풀다운 메뉴 (반복 설정용)
- **Icon** — 24×24 그리드, `currentColor` (checkmark, xmark, plus, chevron 계열)

### 캘린더 기능

- **월 / 주 / 일** 3개 뷰 + `오늘` 버튼
- 할 일 추가·수정·삭제 (우하단 + 버튼 → 바텀 시트)
- 제목 / 날짜 / 시간(하루 종일 토글) / 우선순위 / 반복 / 메모
- **반복**: 반복 안 함 · 매일 · 매주 · 매월
- **반복 항목의 완료는 날짜별로 따로 기록** (`doneDates[]`) — 이번 주에 체크해도
  다음 주 항목은 미완료로 남습니다
- 정렬: 하루 종일 → 시간순 → 우선순위순
- 일 뷰의 현재 시각 표시선

---

## 2. 로그인 / 회원가입 신청 (2차 작업 · 요청하신 부분)

### 관리자 계정

앱을 처음 열 때 자동으로 1개 생성됩니다.

| 이름 | PIN | 역할 | 상태 |
|---|---|---|---|
| 이준호 | 4943 | admin | 승인됨 |

### 로그인 화면

첫 화면이 로그인 화면으로 바뀌었습니다. **아이디 없이 이름 + PIN**만 사용합니다.

- 상단 세그먼트 탭: `로그인` / `회원가입 신청`
- 이름 입력 + PIN 입력(마스킹, `inputmode="numeric"`, 최대 8자)
- 실패 시 빨간 알림 / 신청 접수 시 초록 알림

오류 문구:

| 상황 | 문구 |
|---|---|
| 이름·PIN 불일치 | 이름 또는 PIN이 올바르지 않습니다. |
| 승인 대기 중 | 관리자 승인을 기다리는 중입니다. |
| 거절된 계정 | 가입 신청이 거절된 계정입니다. |

### 회원가입 신청

가입은 **바로 되지 않고 관리자 승인**을 거칩니다.

1. `회원가입 신청` 탭에서 이름 + PIN + PIN 확인 입력
2. 검증
   - 이름 미입력 → `이름을 입력해 주세요.`
   - 중복 이름 → `이미 사용 중인 이름입니다.`
   - PIN 형식 위반 → `PIN은 숫자 4~8자리로 입력해 주세요.`
   - PIN 확인 불일치 → `PIN이 서로 다릅니다.`
3. 통과하면 `status: 'pending'` 으로 저장되고 로그인 탭으로 돌아가며
   `회원가입 신청이 접수되었습니다. 관리자 승인 후 로그인할 수 있습니다.` 표시
4. 승인 전에는 로그인 불가

### 관리자 승인 화면

관리자로 로그인하면 우측 상단 계정 바에 **`회원가입 신청` 버튼**이 생기고,
대기 중인 신청 수가 **빨간 배지**로 표시됩니다.

- 버튼을 누르면 바텀 시트에 신청자 목록(이름 + 신청일)
- 각 행에 `거절` / `수락` 버튼
- 대기 신청이 없으면 `대기 중인 신청이 없습니다`
- 승인·거절 즉시 배지 갱신

### 계정별 할 일 분리

개인 캘린더이므로 **계정마다 할 일 목록이 완전히 분리**됩니다.

- 저장 키: `todo-cal-v1:<사용자 id>`
- 계정 도입 이전에 저장돼 있던 `todo-cal-v1` 데이터는 **관리자 계정이 승계**
- 관리자만 샘플 할 일을 받고, **새로 가입한 회원은 빈 캘린더에서 시작**

### 자동 로그인 (3~4차 수정)

로그인 화면에 **`자동 로그인` 스위치**가 있습니다. 기본값은 **꺼짐**입니다.

- **끈 상태로 로그인** → 저장 안 함. 다음에 열면 다시 로그인 화면
- **켜고 로그인** → `todo-cal-session-v2` 에 저장. 다음에 열면 로그인 화면을
  건너뛰고 바로 달력
- `로그아웃` 을 누르면 저장된 세션이 지워지고 스위치도 다시 꺼집니다
- 할 일 데이터는 로그아웃해도 남아 있어 다시 로그인하면 이어서 사용 가능

> 이전 버전에서 저장된 `todo-cal-session-v1` 은 무시합니다. 스위치를 켠 적이
> 없는데 자동 로그인되는 문제를 막기 위해 키 이름을 `v2` 로 올렸습니다.

---

## 2-1. 입체감·그라데이션을 디자인 기본값으로 (4차 수정)

버튼마다 입체감이 제각각이던 것을 **공통 토큰 3개**로 통일하고, 이제
`.btn` / `.seg` / `.pri` / `.picker-btn` / `.card` 에 **기본으로** 적용됩니다.
따로 지정하지 않아도 새로 만드는 버튼·카드는 같은 모양이 됩니다.

| 토큰 | 역할 |
|---|---|
| `--tc-sheen` | 위→아래 그라데이션 (광택) |
| `--tc-raise` | 입체 그림자 (기본 버튼·카드용) |
| `--tc-raise-sm` | 약한 입체 그림자 (작은 칩·세그먼트용) |

주요 변경:

- **달력 위 월/주/일 3개 버튼** — 선택된 것만 떠 보이고 나머지는 평평했는데,
  이제 **3개 모두** 그라데이션 + 그림자를 가진 버튼 모양입니다.
  선택된 것은 더 밝고 그림자가 강해 구분됩니다.
- **감싸는 카드** — 안쪽으로 파인(inset) 홈처럼 처리해 위의 버튼 3개가
  떠 보이도록 했습니다.
- **로그인 / 회원가입 신청 탭**도 같은 세그먼트 스타일을 씁니다.
- **카드** — 반복되던 인라인 스타일을 `.card` 클래스로 정리했습니다
  (달력 월/주/일 뷰, 할 일 목록, 로그인 상자).
- 다크 모드용 값도 각각 따로 정의했습니다.

> 참고: CSS에서 `background:` (단축 속성)를 쓰면 그라데이션이 지워집니다.
> 색만 바꿀 때는 반드시 **`background-color:`** 를 쓰세요. 기존 버튼 색상
> 지정들도 전부 이걸로 고쳤습니다.

---

## 3. ⚠️ 보안 관련 (반드시 확인해 주세요)

**이 로그인은 보안이 아닙니다.**

- PIN이 `localStorage` 에 **평문 그대로** 저장됩니다
- 검증이 **브라우저 안에서만** 이루어집니다
- 개발자 도구를 열 수 있는 사람은 `4943` 을 그대로 읽거나 로그인 화면을
  **우회**할 수 있습니다

즉, 한 대의 기기를 함께 쓰는 사람끼리 서로의 목록을 안 보이게 하는
**칸막이** 역할이지, 외부 침입을 막는 인증이 아닙니다.

진짜 인증이 필요해지는 시점 = **이 파일이 이 컴퓨터를 벗어날 때**입니다.
그때는 PIN 검증을 서버로 옮기고 해시(bcrypt/argon2)로 저장해야 합니다.
코드에도 해당 위치에 주석으로 표시해 두었습니다 (`index.html` 의 accounts 섹션).

---

## 4. 저장되는 데이터

| 키 | 내용 |
|---|---|
| `todo-cal-users-v1` | 계정 목록 `{id, name, pin, role, status, at}` |
| `todo-cal-session-v2` | 자동 로그인을 켠 경우에만 저장되는 사용자 id |
| `todo-cal-v1:<id>` | 해당 계정의 할 일 목록 |
| `todo-cal-v1` | (구버전) 계정 도입 이전 데이터 — 관리자에게 승계됨 |

`status` 는 `pending` / `approved` / `rejected` 세 가지입니다.

---

## 5. 자체 검사

주소 뒤에 `?selftest` 를 붙이면 **17개 검사**가 실행되고, 통과 시 콘솔에
`selftest: all checks passed` 가 찍힙니다.

검사 범위: 반복 규칙(매일·매주·매월), 날짜별 완료 처리, 정렬·필터링,
PIN 형식 검증, 관리자 계정 시딩, 이름 공백 처리·부분 일치 방지,
계정별 저장 키 분리.

---

## 6. 브라우저에서 확인한 시나리오

1. 이준호 / 4943 로그인 → 캘린더 진입 ✓
2. 로그아웃 → 로그인 화면 복귀 ✓
3. PIN 확인 불일치 → `PIN이 서로 다릅니다.` ✓
4. 김민수 / 1234 가입 신청 → 접수 안내 표시 ✓
5. 승인 전 로그인 시도 → `관리자 승인을 기다리는 중입니다.` ✓
6. 관리자 계정 바 배지 `1` 표시 → `수락` → 배지 사라짐 ✓
7. 김민수 로그인 → **빈 캘린더**(`할 일이 없습니다`)로 진입, 계정 바에
   관리자 메뉴 없음 ✓
8. 자동 로그인 **끄고** 로그인 → 세션 저장 안 됨, 다시 열면 로그인 화면 ✓
9. 자동 로그인 **켜고** 로그인 → 다시 열면 바로 달력 ✓
10. 로그아웃 → 세션 삭제 + 스위치 꺼짐 ✓
11. 모든 버튼·세그먼트·칩·카드에 그라데이션 + 그림자 적용 확인 ✓

---

## 7. 의도적으로 만들지 않은 것

| 생략한 것 | 추가해야 할 시점 |
|---|---|
| 서버 인증 / PIN 해싱 | 이 페이지를 다른 기기·인터넷에 올릴 때 |
| PIN 변경 / 재설정 | 사용자가 PIN을 잊었을 때 (지금은 복구 수단 없음) |
| 계정 삭제 / 목록 관리 | 승인된 회원이 늘어나 정리가 필요할 때 |
| 로그인 시도 횟수 제한 | 서버 인증으로 옮긴 뒤 (클라이언트 제한은 우회됨) |
| 다크 모드 토글 UI | 원본 디자인에도 UI가 없어 시스템 설정을 따름 |
